cd "C:\Users\Mateusz\Desktop\MyApplication\app\build\outputs\apk\debug"; $port=8000; $filename="app-debug.apk"; $listener=[System.Net.Sockets.TcpListener]$port; $listener.Start(); Write-Host "Serwer gotowy. Wpisz w przegladarce telefonu: http://172.20.10.2:$port"; $client=$listener.AcceptTcpClient(); $stream=$client.GetStream(); $fileBytes=[System.IO.File]::ReadAllBytes((Join-Path (Get-Location) $filename)); $header="HTTP/1.1 200 OK`r`nContent-Type: application/vnd.android.package-archive`r`nContent-Length: $($fileBytes.Length)`r`nContent-Disposition: attachment; filename=`"$filename`"`r`nConnection: close`r`n`r`n"; $headerBytes=[System.Text.Encoding]::ASCII.GetBytes($header); $stream.Write($headerBytes, 0, $headerBytes.Length); $stream.Write($fileBytes, 0, $fileBytes.Length); $stream.Flush(); Write-Host "Dane wysłane do bufora sieciowego."; for($i=30; $i -gt 0; $i--) { Write-Progress -Activity "Utrzymywanie połączenia dla telefonu" -Status "Pozostało $i sekund do wyłączenia serwera..."; Start-Sleep -Seconds 1 }; $stream.Close(); $client.Close(); $listener.Stop(); Write-Host "Serwer zamknięty pomyślnie."